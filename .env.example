# Keysoft does not require environment variables for local development.
#
# Never put secrets in EXPO_PUBLIC_* variables: Expo embeds them in the client
# bundle. Keep signing credentials and service tokens in EAS/GitHub secret stores
# or in the ignored local .secrets/ directory.
