/**
 * Contact details used by the app's interactive contact actions. Support is handled over
 * email rather than through a web page, and both values below are the single place where
 * the address and the site are defined.
 */
export const SUPPORT_EMAIL = 'keysoft@mikesoft.it';

/** Host shown to the user; the URL is built from it so the domain is written once. */
export const WEBSITE_HOST = 'www.mikesoft.it';

export const WEBSITE_URL = `https://${WEBSITE_HOST}`;
